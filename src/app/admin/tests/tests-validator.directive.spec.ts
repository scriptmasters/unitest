import {TestsValidatorDirective} from './tests-validator.directive';

describe('TestsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TestsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
